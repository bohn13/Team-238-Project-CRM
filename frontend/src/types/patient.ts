export type Patient = {
  firstName: string,
  lastName:string,
  gender: string,
  dateOfBirth: Date,
  address: string,
  source: string,
  userId: number,
  phoneNumber: string,
  email:string,
}
